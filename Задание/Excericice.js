let x, y;
let m, n;

m = Math.cos(5);
n = Math.sin(666);

//function ctg(x) {return 1 / Math.tan;}

for (x = -3; x <= 3; x = x + 0.2)
{
    y = (Math.tan(Math.pow(x, 6)) - Math.pow(-99,666, -x)) / (Math.cos(Math.tan(Math.pow(1 / (Math.pow((m / n) / n, 2))))) + Math.pow(69,666, m - n));
    console.log(Math.trunc(y));
        
}

if(Number.isSafeInteger(y)){
    alert("True");
}
else{
    alert("False");
}
